import { IEditorOverrideServices } from '../vscode/src/vs/editor/standalone/browser/standaloneServices.js';

interface ExtensionGalleryOptions {
    /**
     * Whether we should only allow for web extensions to be installed, this is generally
     * true if there is no server part.
     */
    webOnly: boolean;
}
declare function getServiceOverride(options?: ExtensionGalleryOptions): IEditorOverrideServices;

export { type ExtensionGalleryOptions, getServiceOverride as default };
