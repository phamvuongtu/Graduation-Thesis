import { IEditorOverrideServices } from '../vscode/src/vs/editor/standalone/browser/standaloneServices.js';

interface AvailableLanguage {
    locale: string;
    languageName?: string;
}
interface LocalizationOptions {
    setLocale(id: string): Promise<void>;
    clearLocale(): Promise<void>;
    availableLanguages: AvailableLanguage[];
}
declare function getServiceOverride(options: LocalizationOptions): IEditorOverrideServices;

export { type AvailableLanguage, type LocalizationOptions, getServiceOverride as default };
