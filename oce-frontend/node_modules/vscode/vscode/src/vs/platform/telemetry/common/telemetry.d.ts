interface ITelemetryData {
    from?: string;
    target?: string;
    [key: string]: any;
}
interface ITelemetryEndpoint {
    id: string;
    aiKey: string;
    sendErrorTelemetry: boolean;
}
declare const enum TelemetryLevel {
    NONE = 0,
    CRASH = 1,
    ERROR = 2,
    USAGE = 3
}

export { type ITelemetryData, type ITelemetryEndpoint, TelemetryLevel };
