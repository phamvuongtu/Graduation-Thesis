import { URI } from '../../../base/common/uri.js';
import { IBaseWorkspace, IRawFileWorkspaceFolder, IRawUriWorkspaceFolder } from '../../workspace/common/workspace.js';

type IStoredWorkspaceFolder = IRawFileWorkspaceFolder | IRawUriWorkspaceFolder;
interface IStoredWorkspace extends IBaseWorkspace {
    folders: IStoredWorkspaceFolder[];
}
interface IWorkspaceFolderCreationData {
    readonly uri: URI;
    readonly name?: string;
}

export type { IStoredWorkspace, IStoredWorkspaceFolder, IWorkspaceFolderCreationData };
